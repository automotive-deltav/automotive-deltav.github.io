═══════════════════════════════════════════════════════════
  DELTAV AUTOMOTIVE - QUICK START GUIDE
═══════════════════════════════════════════════════════════

📦 FILES YOU NEED (11 total):

PUBLIC:
  ✓ index.html
  ✓ booking.html

ADMIN:
  ✓ login.html
  ✓ dashboard.html
  ✓ admin.html
  ✓ schedule.html
  ✓ invoices.html ⭐ FULL FEATURED!
  ✓ inventory.html ⭐ NEW!

SCRIPTS:
  ✓ admin.js
  ✓ invoices.js
  ✓ inventory.js

═══════════════════════════════════════════════════════════
🚀 5-MINUTE SETUP
═══════════════════════════════════════════════════════════

1. CREATE SUPABASE TABLES
   Go to: https://supabase.com → SQL Editor
   Run these 3 queries:

   CREATE TABLE bookings (
     id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
     name TEXT, email TEXT, phone TEXT, reg TEXT,
     date DATE, notes TEXT, status TEXT DEFAULT 'pending',
     viewed BOOLEAN DEFAULT false, created_at TIMESTAMP DEFAULT NOW()
   );

   CREATE TABLE invoices (
     id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
     customer_name TEXT, customer_email TEXT,
     invoice_date DATE, car_reg TEXT, parts JSONB,
     total_amount DECIMAL(10,2), payment_status TEXT DEFAULT 'unpaid',
     created_at TIMESTAMP DEFAULT NOW()
   );

   CREATE TABLE inventory (
     id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
     purchase_date DATE, item_code TEXT, item_name TEXT,
     serial_number TEXT, cost DECIMAL(10,2), supplier TEXT,
     status TEXT DEFAULT 'active', created_at TIMESTAMP DEFAULT NOW()
   );

2. CHANGE LOGIN CREDENTIALS
   Open: login.html
   Edit lines 111-112:
     const ADMIN_USERNAME = "your_username";
     const ADMIN_PASSWORD = "your_secure_password";

3. UPDATE CONTACT INFO
   Open: index.html
   Find and replace:
     - Phone: 0121 XXX XXXX
     - Email: info@deltavautomotive.co.uk

4. DEPLOY TO NETLIFY
   - Go to netlify.com
   - Drag your folder
   - Done!

═══════════════════════════════════════════════════════════
🔐 ADMIN ACCESS
═══════════════════════════════════════════════════════════

Homepage → Triple-click "DeltaV" logo → Login page

(Hidden from regular users!)

═══════════════════════════════════════════════════════════
✨ WHAT WORKS
═══════════════════════════════════════════════════════════

✓ Customer booking with email verification
✓ Admin login system
✓ Manage bookings (confirm/pending/delete)
✓ Calendar/schedule view
✓ FULL invoice system with parts & calculations
✓ Inventory management with returns
✓ Search & filter everything
✓ Custom modals (no ugly alerts)
✓ Auto-refresh
✓ Mobile responsive

═══════════════════════════════════════════════════════════
🎯 INVOICE SYSTEM (MOST IMPORTANT!)
═══════════════════════════════════════════════════════════

✓ Add/Edit/Delete invoices
✓ Multiple parts per invoice
✓ Part numbers, descriptions, qty, price
✓ Auto-calculate totals
✓ Payment status tracking
✓ Customer info
✓ Full database integration

═══════════════════════════════════════════════════════════

Need help? Check README.md for full documentation!

═══════════════════════════════════════════════════════════
